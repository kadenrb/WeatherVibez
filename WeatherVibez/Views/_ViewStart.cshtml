@{
    // Check if user is authenticated
    var isAuthenticated = Context.User?.Identity?.IsAuthenticated ?? false;

    // Set layout based on authentication status
    Layout = isAuthenticated ? "_LayoutLoggedIn" : "_LayoutPublic";
}
